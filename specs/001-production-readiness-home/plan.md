# Implementation Plan: Production Readiness — '92 Subaru Site

**Branch**: `001-production-readiness-home` | **Date**: 2026-07-24 | **Spec**: [spec.md](spec.md)

**Input**: Feature specification from `/specs/001-production-readiness-home/spec.md`

## Summary

Take the existing Deno + vanilla-JS '92 Subaru site from prototype to production: reliable email-delivered bookings (with spam protection and a privacy policy), a real "Soundtrack" (renamed from "Mixtape", no A/B sides, embedded YouTube), a simplified About, an EPK page, and production hosting on Vercel with SEO, analytics, mobile responsiveness, and a themed 404. Gigs is deferred (Day 2). Delivery schedule and per-item acceptance live in [`/BACKLOG.md`](../../BACKLOG.md).

## Technical Context

**Language/Version**: TypeScript on Deno 2.x (server); vanilla HTML/CSS/JS (front-end, no framework).
**Primary Dependencies**: `Deno.serve` (HTTP); Resend for booking email delivery (decided 2026-07-28); Google reCAPTCHA v2 (spam; swapped from Cloudflare Turnstile 2026-07-28); Umami analytics (decided 2026-07-28).
**Storage**: None — email is the system of record. **Deno KV is removed** (incompatible with Vercel).
**Testing**: `deno test` for server logic (validation, email-payload shaping); manual/headless browser check for booking flow, mobile, and soundtrack.
**Target Platform**: Vercel (Deno runtime), served globally; modern browsers + mobile (360–414px).
**Project Type**: Web application (static front-end in `public/` + small Deno API in `server/`).
**Performance Goals**: Fast first load; booking email delivered < 1 min (SC-001); 0ms zero-latency audio playback startup via pre-decoded Web Audio API ArrayBuffers (`AudioContext.decodeAudioData`).
**Constraints**: ~3-day launch window; no cookie-consent banner (privacy-friendly analytics); motion kept.
**Scale/Scope**: Small brochure + booking site; 4 pages (Home, About, Contact/Book, EPK) + 404 + privacy.

## Constitution Check

Constitution is the Spec Kit default template (`.specify/memory/constitution.md`, not yet customized). No project-specific gates defined → no violations. Baseline engineering principles apply: keep the front-end dependency-free, validate server-side, don't log PII beyond the booking email, and keep secrets in env vars.

## Project Structure

### Documentation (this feature)

```
specs/001-production-readiness-home/
├── spec.md
├── plan.md              # this file
└── checklists/          # generated by /speckit-checklist
```

### Source Code (repository root)

```
server/
  main.ts               # Deno.serve: static + booking endpoint (email); KV removed
  email.ts              # (new) booking → email delivery
public/
  index.html            # Home (hero, Soundtrack, About/Contact views) + EPK
  app.js                # routing, soundtrack (YouTube), booking form
  styles.css
  404.html              # (new) themed 404
```

**Structure Decision**: Keep the existing `server/` + `public/` split. Remove the KV datastore (`server/data.ts` booking store + `data_test.ts` KV bits); add an email module and a themed 404. Front-end stays framework-free.

## Complexity Tracking

| Area | Note |
|---|---|
| Deno on Vercel | Vercel is not a first-class Deno host; needs a runtime/adapter or Edge/Node shim. Validate early (D1-17) — highest-risk item. |
| Email deliverability | Requires a provider + verified sender domain; the real address is owner-provided (D1-11). |
| Owner-provided content | Copy, YouTube links, socials, EPK bio/photos are external inputs; tracked as `NEEDS CLARIFICATION` in the spec. |
